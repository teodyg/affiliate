export default function BeginQualifyButton({ id, className }) {
    return (
        <a 
            id={id}
            href="/login" 
            className={"button w-button" + ' ' + className} 
            data-text="t3838c96c" 
            data-link="a5ac3fee2"
        >
            Sign in
        </a>
    )
}